using System;
using System.Collections.Generic;
using UnityEngine;

// Base class for tower on the map that fires a projectile to enemy characters in range and that also can be destroyed by them
public class Tower : MonoBehaviour
{
    // Need to think this through
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
